// Prerendered: this page is pure static prose (lib/content/levels.ts) with no API call at all,
// so there is nothing about it that has to wait for JavaScript. See src/routes/+page.ts.
export const prerender = true;
