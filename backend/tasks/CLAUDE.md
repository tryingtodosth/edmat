# tasks — management step (MANAGEMENT-BRIEF.md §3)

Skeleton registered by the prep commit. The step's agent replaces this file with the app's own
contract: the shape, the invariants, the rule module, the refusal words and what is deliberately
not here.
